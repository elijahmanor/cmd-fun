# Miscellaneous Tips & Tricks

---

## Free up disk space from npm, yarn, brew, ..

<text top={5}>
    npm cache clean --force # clans up npm yarn cache dir # shows where cache is
    stored yarn cache list # lists everything in cache yarn cache clean # cleans
    the cache brew uninstall yarn # uninstall yarn brew cleanup -n # shows what
    would cleanup brew cleanup # cleans up brew TODO: Need command here # cleans
    up docker
</text>
